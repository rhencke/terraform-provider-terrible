name: Build Nuitka binary
description: >
  Compile the provider with Nuitka. Assumes uv dependencies are installed
  and ccache is set up (run setup-ccache first).

runs:
  using: composite
  steps:
    - name: Build
      shell: bash
      run: |
        uv run python -m nuitka \
          --onefile \
          --jobs=1 \
          --assume-yes-for-downloads \
          --include-package=ansible \
          --include-package=tf \
          --include-package=grpc \
          --include-package=jinja2 \
          --include-package=cryptography \
          --include-package=terrible_provider \
          --include-package-data=ansible \
          --include-package-data=tf \
          --include-package-data=jinja2 \
          --include-package-data=cryptography \
          --output-filename=terraform-provider-terrible \
          --output-dir=dist \
          _terrible_main.py

    - name: ccache stats
      shell: bash
      run: ccache --show-stats
